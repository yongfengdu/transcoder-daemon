Verified on Ubuntu 16.04

cd kernel;./install-kernel.sh # To install linux kernel with transcode support
reboot # to use new kernel
cd k8s;./runk8s.sh # To setup k8s env and run transcode container

copy your files to /opt/media/$type directory to transcode 
